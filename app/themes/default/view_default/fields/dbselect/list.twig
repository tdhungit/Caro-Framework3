{% if attribute(row, name) %}
    {% set select_data = utils.getDbDropDownList(field.options, '', attribute(row, name)) %}
    {% set value = select_data['name'] %}
{% else %}
    {% set value = '' %}
{% endif %}